To sync clock between Windows and Linux `timedatectl set-local-rtp true`
To get bigger font in Spotify add `--force-device-scale-factor=2` to `/usr/share/applications/spotify.desktop`, line `Exec=spotify %U`
