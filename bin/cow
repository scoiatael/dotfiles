#!/bin/sh

set -e 

BUILD_DIR=$HOME/Build

case $1 in
    build)
        shift
        echo "Building $1"
        cd $BUILD_DIR
        cow -fd $1
        cd $1
        vi PKGBUILD
        makepkg -si
        ;;
    *)
        exec cower -cauto $@;;
esac

